/* Google Business Profile.
 *
 * Esporta:
 *   urlAutorizzazione(orgId)              dove mandare il cliente per autorizzare
 *   scaricaNuoveRecensioni(quante)        legge le recensioni nuove
 *   pubblicaRisposta(reviewId, testo)     scrive la risposta
 *
 * Per ora sono finte: restituiscono dati inventati.
 * Tieni le firme cosi' come sono: quando avrai le credenziali OAuth
 * riscrivi solo il contenuto di queste tre funzioni e nient'altro
 * nel progetto deve cambiare.
 *
 * Il token OAuth di ogni cliente va salvato nel database, legato al suo
 * org_id, e non deve mai arrivare al frontend.
 */
