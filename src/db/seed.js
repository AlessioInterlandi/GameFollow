/* Popola il database con dati finti per lo sviluppo.
 *
 * Crea almeno DUE aziende diverse, ognuna con un utente e alcune recensioni.
 * Due e non una: e' l'unico modo per accorgerti con gli occhi se
 * l'isolamento tra clienti funziona davvero.
 *
 * Le password vanno salvate passando da services/password.js,
 * mai in chiaro nemmeno nei dati finti.
 */
